"""Template for Loam secrets.

Copy this file to secrets.py and fill in real values:

    cp secrets.py.example secrets.py

secrets.py is gitignored and must never be committed. const.py imports from it
at runtime and falls back to empty strings if it is not present.
"""

TOMORROW_API_KEY = "your-tomorrow-io-key-here"
